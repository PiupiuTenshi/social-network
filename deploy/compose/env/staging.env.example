COMPOSE_PROJECT_NAME=twilight-staging
INFRA_ENV=staging
KAFKA_TOPIC_PREFIX=twilight-staging
# Create the remaining required secret values through the staging secret store; never copy dev credentials.
